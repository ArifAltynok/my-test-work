const Apitest = () => {

    const Dogimages = () => {
        const options = {
            method: 'GET',
            url: 'https://dog.ceo/api/breeds/image/random'
        }

    };

    return <button> onClick = {Dogimages} button </button>;

};

export default Apitest;